server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /SWE-Jobs/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /SWE-Jobs/index.html;
    }

    location / {
        return 301 /SWE-Jobs/;
    }
}
